services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  deploy-service:
    build: ./vercel-deploy-service
    env_file:
      - ./vercel-deploy-service/.env
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
